# Qaio Companion — environment variables
# Copy to `.env` (or `.env.local`) and tweak as needed.

# WebSocket relay URL. Omit/blank to use the public default:
# wss://qaio-relay.julianarango1818.workers.dev
VITE_RELAY_URL=wss://qaio-relay.julianarango1818.workers.dev
